apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: echo 
spec:
  params:
    - name: message
      description: the message to echo
      type: string
  steps:
    - name: echo
      image: alpine:3
      command: [/bin/echo]
      args: ["$(params.message)"]
